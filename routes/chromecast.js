/**
 * Chromecast Router
 */
var express = require('express');
var response = require('../utils/response');
var chromecastController = require('../controllers/chromecastController');

var router = express.Router();

router.get('/', (req, res) => {
    
});

module.exports = router;